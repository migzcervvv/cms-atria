// This file is intentionally safe as an empty starter registry.
// Running `npx boneyard-js build` or the Vite plugin can regenerate it with captured bones.
import { registerBones } from "boneyard-js/react";

registerBones({});
