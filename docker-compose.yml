services:
  mongodb:
    image: mongo:7
    container_name: cms-atria-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
